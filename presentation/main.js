// document.querySelectorAll("pre code").forEach((e) => {
//     const html = e.innerHTML;
//     const pattern = html.match(/\s*\n[\t\s]*/);
//     e.innerHTML = html.replace(new RegExp(pattern, "g"), "\n");
// });
